<Window x:Class="app.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"

        mc:Ignorable="d"
        Title="EditWindow" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen"  WindowState="Maximized">

    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="20*"/>

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>


        <TextBox x:Name="tbxTitle" Text="" Grid.Row="0" Grid.Column="0" Margin="20" Style="{StaticResource MaterialDesignOutlinedTextBox}" FontSize="16" FontWeight="Bold" />
        <TextBox x:Name="tbxDetails" Margin="20" Grid.Row="1" Grid.Column="0" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top" FontSize="16" />
        <Button x:Name="btnSave" Content="save" Grid.Column="1" Grid.Row="2" Click="btnSave_Click" />
        <Button x:Name="btnDelete" Grid.Column="0" Grid.Row="2" Content="Delete" Width="80" Height="30" Click="btnDelete_Clickk" />
    </Grid>



</Window>
